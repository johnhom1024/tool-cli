{
  "name": "tool-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:add": "changeset add",
    "changeset:version": "changeset version",
    "changeset:publish": "turbo deploy && changeset publish",
    "turbo:build": "turbo build"
  },
  "keywords": [],
  "author": "johnhom",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.8.8"
  }
}
